server:
  port: 9092

eureka:
  instance:
    hostname: localhost
    port: 9091
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 3000

security:
    oauth2:
        client:
            access-token-validity-seconds: 1800
            authorized-grant-types: password,refresh_token
            client-id: tceauth
            client-secret: $2a$10$4CvdsdqhNu/A1ERtlyqOYeSbwnRbL7xCbPclZ7k3o6HvWw0oU3v1u
            resource-ids: resources
            scope: read,write

ldap.enabled: "true"
ldap.urls: ldap://dcsede1.tce.pe:389/
ldap.base.dn: dc=tce,dc=pe
ldap.username: cn=SVC_GDSI,ou=Servico,dc=tce,dc=pe
ldap.password: '!123Mudar#'
ldap.user.dn.pattern: uid={0}